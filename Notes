provide ability to user so that no other user can delete ther post 
JWT -> JASON WEB Token provide unique token which user can use instead of using session cookies 

const accessToken = jwt.sign({id:user.id,isAdmin:user.isAdmin},"mySecretKey")
this is the command jwt.sign which we use to create a hash encrypted 